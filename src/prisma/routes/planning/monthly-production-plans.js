const router = require("express").Router();
const ctrl = require("../../controllers/planning/MonthlyProductionPlanController");
const { authorize } = require("../../middleware/auth");
const { logger } = require("../../middleware/logger");
const { approvalGate } = require("../../services/approvalRuleService");
const { guardMps, guardPlan } = require("../../middleware/planningPeriodGuard");
const monthlyPlanApproval = approvalGate({ moduleCode: "planning-ppic", pageCode: "monthly-plan", actionCode: "approve", documentType: "MonthlyProductionPlan", param: "planNumber", model: "monthlyProductionPlan", lookupField: "planNumber", numberField: "planNumber" });
const capacityOverrideApproval = approvalGate({ moduleCode: "planning-ppic", pageCode: "monthly-plan", actionCode: "capacity-override", documentType: "MonthlyProductionPlan", param: "planNumber", model: "monthlyProductionPlan", lookupField: "planNumber", numberField: "planNumber" });
router.post("/from-mps", authorize("monthlyProductionPlan", "create"), guardMps, logger("monthlyProductionPlan", "create-from-mps"), ctrl.createFromMps);
router.post("/:planNumber/confirm", authorize("monthlyProductionPlan", "approve"), guardPlan, monthlyPlanApproval, logger("monthlyProductionPlan", "confirm"), ctrl.confirm);
router.post("/:planNumber/release", authorize("monthlyProductionPlan", "release"), guardPlan, logger("monthlyProductionPlan", "release"), ctrl.release);
router.post("/:planNumber/daily-plans", authorize("monthlyProductionPlan", "release"), guardPlan, logger("monthlyProductionPlan", "convert-to-daily-plans"), ctrl.convertToDailyPlans);
router.post("/:planNumber/manual-daily-plans", authorize("monthlyProductionPlan", "release"), guardPlan, logger("monthlyProductionPlan", "manual-daily-plan"), ctrl.createManualDailyPlan);
router.post("/:planNumber/manual-allocations", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "manual-allocation"), ctrl.createManualDailyPlan);
router.get("/:planNumber/capacity-flow-rule", authorize("monthlyProductionPlan", "read"), ctrl.getCapacityFlowRule);
router.put("/:planNumber/capacity-flow-rule", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "capacity-flow-rule"), ctrl.saveCapacityFlowRule);
router.post("/:planNumber/capacity-recommendation", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "capacity-recommendation"), ctrl.recommendCapacity);
router.post("/:planNumber/capacity-adopt-simulation", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "capacity-adopt-simulation"), ctrl.adoptCapacitySimulation);
router.patch("/:planNumber/manual-allocations/:allocationId", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "edit-manual-allocation"), ctrl.updateManualAllocation);
router.patch("/:planNumber/manual-allocations/:allocationId/remove", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "remove-manual-allocation"), ctrl.removeManualAllocation);
router.post("/:planNumber/capacity-override", authorize("monthlyProductionPlan", "approve"), guardPlan, capacityOverrideApproval, logger("monthlyProductionPlan", "capacity-override"), ctrl.capacityOverride);
router.post("/:planNumber/capacity-machine-override", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "capacity-machine-override"), ctrl.assignCapacityMachine);
router.post("/capacity-day", authorize("monthlyProductionPlan", "update"), logger("monthlyProductionPlan", "capacity-day-global"), ctrl.setGlobalCapacityDay);
router.get("/daily-plans", authorize("monthlyProductionPlan", "read"), ctrl.listDailyPlans);
router.get("/daily-plans/:scheduleNumber", authorize("monthlyProductionPlan", "read"), ctrl.getDailyPlan);
router.post("/:planNumber/capacity-day", authorize("monthlyProductionPlan", "update"), guardPlan, logger("monthlyProductionPlan", "capacity-day"), ctrl.setCapacityDay);
router.get("/:planNumber/material-readiness", authorize("monthlyProductionPlan", "read"), ctrl.materialReadiness);
router.get("/", authorize("monthlyProductionPlan", "read"), ctrl.list);
router.get("/:planNumber", authorize("monthlyProductionPlan", "read"), ctrl.get);
module.exports = router;
