require("dotenv").config({ quiet: true });
const { prisma } = require("../src/prisma");
const ctrl = require("../src/prisma/controllers/purchasing/PurchaseSuggestionController");
function invoke(runNumber) { return new Promise((resolve, reject) => { const req = { params: { runNumber }, body: {}, user: { username: "real-test" } }; const res = { statusCode: 200, status(c) { this.statusCode = c; return this; }, json(v) { resolve({ statusCode: this.statusCode, body: v }); } }; Promise.resolve(ctrl.generate(req, res, (e) => reject(e))).catch(reject); }); }
(async () => { const runs = await prisma.mRPRun.findMany({ where: { isDeleted: false, status: "Completed", isCurrentPlan: true }, select: { runNumber: true }, orderBy: { runNumber: "asc" } }); const out = []; for (const run of runs) { const r = await invoke(run.runNumber); out.push({ runNumber: run.runNumber, statusCode: r.statusCode, suggestionNumber: r.body?.suggestionNumber, status: r.body?.status }); } console.log(JSON.stringify(out, null, 2)); })().catch((e) => { console.error(e); process.exitCode = 1; }).finally(() => prisma.$disconnect());
