require("dotenv").config({ quiet: true });
const { prisma } = require("../src/prisma");
(async () => { const rows = await prisma.manufacturingOrder.findMany({ where: { isDeleted: false }, include: { workOrders: { where: { isDeleted: false }, select: { woNumber: true, sequence: true, status: true, qtyProduced: true, qtyGood: true } }, qualityInspections: { where: { isDeleted: false }, select: { inspectionNumber: true, status: true, decision: true, qtyPassed: true, productionLogId: true, woId: true } } }, orderBy: { moNumber: "asc" } }); console.log(JSON.stringify(rows.map((row) => ({ moNumber: row.moNumber, status: row.status, qtyPlanned: row.qtyPlanned, qtyGood: row.qtyGood, workOrders: row.workOrders, qcs: row.qualityInspections })), null, 2)); })().catch((e) => { console.error(e); process.exitCode = 1; }).finally(() => prisma.$disconnect());
